/// The hosted Privacy Policy URL opened by the "Privacy Policy" entry in
/// `SetupScreen`'s overflow menu (Phase 6). **Placeholder** — swap this
/// for the real URL once the policy (drafted in `privacy_policy/`, see
/// PHASE6_PRELAUNCH_PREP.md) is hosted somewhere public (e.g. GitHub
/// Pages). This is the only place that needs to change; nothing else in
/// the app references the URL directly.
const String privacyPolicyUrl =
    'https://example.com/tabletennis-scoreboard/privacy-policy';
